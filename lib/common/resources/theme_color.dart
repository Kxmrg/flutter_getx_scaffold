import 'package:flutter/material.dart';
import 'package:getx_scaffold/getx_scaffold.dart';

class ThemeColor {
  static get primary => Theme.of(Get.context!).colorScheme.primary;
  static get onPrimary => Theme.of(Get.context!).colorScheme.onPrimary;
  static get primaryContainer =>
      Theme.of(Get.context!).colorScheme.primaryContainer;
  static get onPrimaryContainer =>
      Theme.of(Get.context!).colorScheme.onPrimaryContainer;
  static get secondary => Theme.of(Get.context!).colorScheme.secondary;
  static get onSecondary => Theme.of(Get.context!).colorScheme.onSecondary;
  static get secondaryContainer =>
      Theme.of(Get.context!).colorScheme.secondaryContainer;
  static get onSecondaryContainer =>
      Theme.of(Get.context!).colorScheme.onSecondaryContainer;
  static get tertiary => Theme.of(Get.context!).colorScheme.tertiary;
  static get onTertiary => Theme.of(Get.context!).colorScheme.onTertiary;
  static get tertiaryContainer =>
      Theme.of(Get.context!).colorScheme.tertiaryContainer;
  static get onTertiaryContainer =>
      Theme.of(Get.context!).colorScheme.onTertiaryContainer;
  static get error => Theme.of(Get.context!).colorScheme.error;
  static get onError => Theme.of(Get.context!).colorScheme.onError;
  static get errorContainer =>
      Theme.of(Get.context!).colorScheme.errorContainer;
  static get onErrorContainer =>
      Theme.of(Get.context!).colorScheme.onErrorContainer;
  static get surface => Theme.of(Get.context!).colorScheme.surface;
  static get onSurface => Theme.of(Get.context!).colorScheme.onSurface;
  static get onSurfaceVariant =>
      Theme.of(Get.context!).colorScheme.onSurfaceVariant;
  static get outline => Theme.of(Get.context!).colorScheme.outline;
  static get outlineVariant =>
      Theme.of(Get.context!).colorScheme.outlineVariant;
  static get shadow => Theme.of(Get.context!).colorScheme.shadow;
  static get scrim => Theme.of(Get.context!).colorScheme.scrim;
  static get inverseSurface =>
      Theme.of(Get.context!).colorScheme.inverseSurface;
  static get onInverseSurface =>
      Theme.of(Get.context!).colorScheme.onInverseSurface;
  static get inversePrimary =>
      Theme.of(Get.context!).colorScheme.inversePrimary;
  static get surfaceTint => Theme.of(Get.context!).colorScheme.surfaceTint;
}
